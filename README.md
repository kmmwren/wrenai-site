# wrenai.uk

The public website for **WRENAI LTD** (company no. 17221546), served via GitHub Pages
on the custom domain `wrenai.uk`.

- `index.html` — self-contained single-page site (all CSS inline, no external deps).
  Visual design produced via ChatGPT (Codex), copy approved by Katherine 2026-06-04.
- `CNAME` — custom domain for GitHub Pages.

To update: edit `index.html`, commit, push to `main`. GitHub Pages redeploys automatically.
